Task 7: Orchestrator Data Flow Fix — Evidence
==============================================
Date: 2026-04-18

## Changes Made

### pipeline/orchestrator.py
1. **dict vs ORM fix (line 65-80)**: `fetch_category_top()` may return `list[dict]` 
   (e.g. from mocks or future API changes). Added `isinstance(bm, dict)` check — 
   dicts are filtered to valid AmazonBenchmark columns, skipping entries without 
   `competitor_asin` (NOT NULL constraint). ORM objects pass through unchanged.

2. **Data flow fix (line 192-193)**: `run_full_pipeline()` now captures 
   `step_analyze()` return value and passes it as `analysis_results` kwarg to 
   `step_plan()`. Previously the return was silently discarded.

3. **step_plan signature (line 89)**: Extended to 
   `step_plan(project_id: int, analysis_results: dict | None = None)`.
   Logs received keys. Wave 2 T8 will consume this data for real planning.

### tests/test_data_flow.py (NEW)
- T7-1: step_analyze handles dict benchmarks without AttributeError
- T7-2: run_full_pipeline passes analyze results to step_plan
- T7-3: step_plan accepts analysis_results kwarg (+ backward compat)

## Test Results
28/28 passed (test_data_flow: 4, test_wave1_bugs: 16, test_e2e_tws: 8, ...)
Zero regressions.

## Constraints Honored
- step_analyze() public return structure unchanged (dict with asin_detail, category_top, competitor_analysis)
- No existing step_* functionality broken
- No new external dependencies
- TDD flow: RED → GREEN
